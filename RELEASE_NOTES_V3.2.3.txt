Sudoku Studio Online V3.2.3

修复：
1. 修复选中空格后点击数字无法填写的问题。根因是 inputNum/clearCell 调用的 pushUndo() 在 V3.2.2 中缺失。
2. 补齐 pushUndo()：记录最多200步撤销快照，并在新操作后清空 redo 栈。
3. 数字键与清除键显式设为 type=button。
4. 棋盘改为视口自适应：同时受可用宽度和浏览器高度约束。
5. 桌面端右侧提示/回放区域独立滚动，优先保证棋盘和主操作区在一屏内。
6. 增加 1180px、900px、560px 及低高度屏幕的响应式规则。
